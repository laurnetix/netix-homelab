---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: radarr
spec:
  interval: 1h
  path: ./kubernetes/apps/media/radarr/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  dependsOn:
    # The NFS claim must be Bound before Radarr can schedule - it cannot import
    # without the library.
    - name: media-nfs
      namespace: media
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: true
