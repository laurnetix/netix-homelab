---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: sonarr
spec:
  interval: 1h
  path: ./kubernetes/apps/media/sonarr/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  dependsOn:
    # The NFS claim must be Bound before Sonarr can schedule - it cannot import
    # without the library.
    - name: media-nfs
      namespace: media
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: true
